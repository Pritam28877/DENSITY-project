import type { NextPage } from "next";
import { Button } from "@mui/material";

const Frame4: NextPage = () => {
  return (
    <Button sx={{ width: 1440 }} variant="contained" color="primary">
      Track and watch yourself progress every day.
    </Button>
  );
};

export default Frame4;
